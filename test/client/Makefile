CC = gcc
PROGRAM = client
CFLAGS = -Wall
#CFLAGS = -g -O2 -Wall -ansi -pedantic
LFLAGS =
#LFLAGS = -L libkatalog -lm -lfoo -lbar
SOURCE = client.c
$(PROGRAM): $(SOURCE)
	$(CC) $(CFLAGS) $(SOURCE) $(LFLAGS) -o $(PROGRAM)

